import { requireAdminRequest } from '@/lib/admin/api-guard';import { audit } from '@/lib/admin/audit';import { errorResponse } from '@/lib/api/errors';import { z } from 'zod';
const schema=z.object({id:z.string().uuid().optional(),slug:z.string().min(2),name:z.string().min(2),description:z.string().optional().nullable(),provider_type:z.string(),base_url:z.string().url(),enabled:z.boolean().default(true),credential_strategy:z.enum(['priority','round_robin']).default('priority'),plan_ids:z.array(z.string().uuid()).optional()});
export async function GET(req:Request){try{const {client}=await requireAdminRequest(req);const {data,error}=await client.from('managed_providers').select('*,provider_models(*),provider_credentials(id,label,secret_ref,enabled,priority,health_status,last_checked_at,last_error)').order('name');if(error)throw error;return Response.json({providers:data})}catch(e){return errorResponse(e)}}
export async function POST(req:Request){try{const {user,client}=await requireAdminRequest(req),b=schema.parse(await req.json());const {plan_ids,...values}=b;const {data,error}=await client.from('managed_providers').insert(values).select().single();if(error)throw error;if(plan_ids){await client.from('provider_plan_access').insert(plan_ids.map(plan_id=>({provider_id:data.id,plan_id,enabled:true})))}await audit(client,req,user.id,'provider.create','provider',data.id,null,data);return Response.json(data,{status:201})}catch(e){return errorResponse(e)}}
export async function PATCH(req:Request){try{const {user,client}=await requireAdminRequest(req),b=schema.extend({id:z.string().uuid()}).parse(await req.json());const {data:before}=await client.from('managed_providers').select('*').eq('id',b.id).single();const {id,plan_ids,...values}=b;const {data,error}=await client.from('managed_providers').update(values).eq('id',id).select().single();if(error)throw error;if(plan_ids){await client.from('provider_plan_access').delete().eq('provider_id',id);if(plan_ids.length)await client.from('provider_plan_access').insert(plan_ids.map(plan_id=>({provider_id:id,plan_id,enabled:true})))}await audit(client,req,user.id,'provider.update','provider',id,before,data);return Response.json(data)}catch(e){return errorResponse(e)}}
