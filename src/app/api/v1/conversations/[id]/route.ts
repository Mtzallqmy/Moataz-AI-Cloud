import { requireApiContext } from '@/lib/auth/api-context';import { ApiError,errorResponse } from '@/lib/api/errors';
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){try{const {user,client}=await requireApiContext(req),{id}=await params;const {data:c}=await client.from('conversations').select('*').eq('id',id).eq('user_id',user.id).maybeSingle();if(!c)throw new ApiError('NOT_FOUND','Conversation not found',404);const {data:messages}=await client.from('messages').select('*').eq('conversation_id',id).eq('user_id',user.id).order('created_at');return Response.json({...c,messages:messages??[]})}catch(e){return errorResponse(e)}}
export async function DELETE(req:Request,{params}:{params:Promise<{id:string}>}){try{const {user,client}=await requireApiContext(req),{id}=await params;await client.from('conversations').delete().eq('id',id).eq('user_id',user.id);return new Response(null,{status:204})}catch(e){return errorResponse(e)}}
