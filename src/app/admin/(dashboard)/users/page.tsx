import { requireAdmin } from '@/lib/admin/guard';
export default async function Page(){const {client:db}=await requireAdmin();const {data:profiles}=await db.from('profiles').select('id,display_name,status,created_at,user_plans(status,plan:plans(name,slug)),user_quotas(*)').order('created_at',{ascending:false}).limit(100);return <><h1>Users</h1><p className="muted">Search and mutations are also available through /api/v1/admin/users.</p><div className="card"><table className="table"><thead><tr><th>User</th><th>ID</th><th>Plan</th><th>Status</th><th>Created</th></tr></thead><tbody>{profiles?.map((u:any)=><tr key={u.id}><td>{u.display_name||u.id.slice(0,8)}</td><td>{u.id}</td><td>{u.user_plans?.find((p:any)=>p.status==='active')?.plan?.name||'—'}</td><td><span className="pill">{u.status}</span></td><td>{new Date(u.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></>}
