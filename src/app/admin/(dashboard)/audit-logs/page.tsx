import { requireAdmin } from '@/lib/admin/guard';export default async function Page(){const {client}=await requireAdmin();const {data}=await client.from('admin_audit_logs').select('*').order('created_at',{ascending:false}).limit(200);return <><h1>Audit Logs</h1><div className="card"><table className="table"><thead><tr><th>Time</th><th>Admin</th><th>Action</th><th>Target</th></tr></thead><tbody>{data?.map((x:any)=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString()}</td><td>{x.admin_user_id.slice(0,8)}</td><td>{x.action}</td><td>{x.target_type}:{x.target_id}</td></tr>)}</tbody></table></div></>}
