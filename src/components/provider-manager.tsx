'use client';
import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
type Plan={id:string;name:string;slug:string};type Credential={id:string;provider_id:string;label:string;source:'database'|'environment';key_mask:string|null;secret_ref:string|null;enabled:boolean;priority:number;health_status:string;last_checked_at:string|null;last_used_at:string|null;failure_count:number;last_error:string|null};type Provider={id:string;name:string;slug:string;description:string|null;provider_type:string;base_url:string;enabled:boolean;priority:number;failover_enabled:boolean;credential_strategy:'priority'|'round_robin';provider_models?:Array<{id:string}>;provider_plan_access?:Array<{plan_id:string;enabled:boolean;plan?:Plan}>;credentials:Credential[]};
async function api(path:string,method:string,body?:unknown){const res=await fetch(path,{method,headers:{'content-type':'application/json'},body:body===undefined?undefined:JSON.stringify(body),cache:'no-store'});const json=await res.json().catch(()=>({}));if(!res.ok)throw new Error(json?.error?.message||`Request failed (${res.status})`);return json}const value=(fd:FormData,key:string)=>String(fd.get(key)||'').trim();
function CredentialRow({credential,run,setMessage}:{credential:Credential;run:(id:string,task:()=>Promise<unknown>)=>Promise<void>;setMessage:(m:string)=>void}){const [replacement,setReplacement]=useState('');return <div className="credential-row"><div><strong>{credential.label}</strong><div className="muted">{credential.source==='database'?(credential.key_mask||'••••••••'):credential.secret_ref} · Priority {credential.priority} · {credential.enabled?'Active':'Inactive'}</div><div className="muted">Health: {credential.health_status} · Failures: {credential.failure_count}{credential.last_used_at?` · Used ${new Date(credential.last_used_at).toLocaleString()}`:''}</div>{credential.last_error&&<div className="muted">Last error: {credential.last_error}</div>}</div><div className="stack"><div className="credential-actions"><button className="btn" type="button" onClick={()=>run(`test-${credential.id}`,async()=>{const r:any=await api(`/api/v1/admin/credentials/${credential.id}/test`,'POST');setMessage(r.message)})}>Test Connection</button><button className="btn" type="button" onClick={()=>run(`toggle-${credential.id}`,()=>api(`/api/v1/admin/credentials/${credential.id}`,'PATCH',{enabled:!credential.enabled}))}>{credential.enabled?'Disable':'Enable'}</button><button className="btn danger" type="button" onClick={()=>{if(confirm(`Delete credential ${credential.label}?`))run(`delete-${credential.id}`,()=>api(`/api/v1/admin/credentials/${credential.id}`,'DELETE'))}}>Delete</button></div>{credential.source==='database'&&<div className="key-input-row"><input className="input" type="password" autoComplete="new-password" value={replacement} onChange={e=>setReplacement(e.target.value)} placeholder="New API key for rotation"/><button className="btn" type="button" disabled={!replacement} onClick={()=>run(`rotate-${credential.id}`,async()=>{await api(`/api/v1/admin/credentials/${credential.id}`,'PATCH',{api_key:replacement});setReplacement('')})}>Replace Key</button></div>}</div></div>}
function AddCredentialForm({providerId,busy,run}:{providerId:string;busy:string;run:(id:string,task:()=>Promise<unknown>,form?:HTMLFormElement)=>Promise<void>}){const [source,setSource]=useState<'database'|'environment'>('database');const [show,setShow]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,fd=new FormData(form);const body:any={provider_id:providerId,label:value(fd,'label'),priority:Number(value(fd,'priority')||100),enabled:fd.get('enabled')==='on',source};if(source==='database')body.api_key=value(fd,'api_key');else body.secret_ref=value(fd,'secret_ref');await run(`add-${providerId}`,()=>api('/api/v1/admin/credentials','POST',body),form)}return <form className="stack add-key" onSubmit={submit}><h3>Add API Credential</h3><input className="input" name="label" placeholder="Key 1" required/><details><summary>Credential source: {source==='database'?'Stored securely':'Environment variable (legacy)'}</summary><select className="input" value={source} onChange={e=>setSource(e.target.value as 'database'|'environment')}><option value="database">Stored securely (default)</option><option value="environment">Environment variable (legacy)</option></select></details>{source==='database'?<div className="key-input-row"><input className="input" name="api_key" type={show?'text':'password'} autoComplete="new-password" placeholder="Paste API key" required/><button type="button" className="btn" onClick={()=>setShow(v=>!v)}>{show?'Hide':'Show'}</button></div>:<input className="input" name="secret_ref" placeholder="NVIDIA_API_KEY_1" required/>}<input className="input" name="priority" type="number" defaultValue="100"/><label><input type="checkbox" name="enabled" defaultChecked/> Active</label><button className="btn" disabled={busy===`add-${providerId}`}>{busy===`add-${providerId}`?'Saving…':'Save Credential'}</button></form>}
export default function ProviderManager({providers,plans}:{providers:Provider[];plans:Plan[]}){const router=useRouter();const [busy,setBusy]=useState('');const [message,setMessage]=useState('');async function run(id:string,task:()=>Promise<unknown>,form?:HTMLFormElement){setBusy(id);setMessage('');try{await task();form?.reset();setMessage('Saved');router.refresh()}catch(e){setMessage(e instanceof Error?e.message:'Request failed')}finally{setBusy('')}}async function createProvider(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,fd=new FormData(form);await run('new-provider',()=>api('/api/v1/admin/providers','POST',{name:value(fd,'name'),slug:value(fd,'slug'),provider_type:value(fd,'provider_type'),base_url:value(fd,'base_url'),description:value(fd,'description')||null,priority:Number(value(fd,'priority')||100),failover_enabled:fd.get('failover_enabled')==='on',credential_strategy:value(fd,'credential_strategy')||'priority',enabled:true,plan_ids:fd.getAll('plan_ids').map(String)}),form)}async function updateProvider(e:FormEvent<HTMLFormElement>,p:Provider){e.preventDefault();const fd=new FormData(e.currentTarget);await run(`provider-${p.id}`,()=>api('/api/v1/admin/providers','PATCH',{id:p.id,name:value(fd,'name'),slug:value(fd,'slug'),provider_type:value(fd,'provider_type'),base_url:value(fd,'base_url'),description:value(fd,'description')||null,priority:Number(value(fd,'priority')||100),failover_enabled:fd.get('failover_enabled')==='on',credential_strategy:value(fd,'credential_strategy')||'priority',enabled:fd.get('enabled')==='on',plan_ids:fd.getAll('plan_ids').map(String)}))}
return <div className="stack"><form className="card stack" onSubmit={createProvider}><h2>Add Provider</h2><div className="provider-form-grid"><input className="input" name="name" placeholder="Moataz NVIDIA" required/><input className="input" name="slug" placeholder="nvidia" required/><input className="input" name="provider_type" defaultValue="openai_compatible" required/><input className="input" name="base_url" placeholder="https://integrate.api.nvidia.com/v1" required/><input className="input" name="priority" type="number" defaultValue="100"/><select className="input" name="credential_strategy"><option value="priority">Priority / failover</option><option value="round_robin">Round robin</option></select></div><textarea className="input" name="description" placeholder="Description"/><label><input type="checkbox" name="failover_enabled" defaultChecked/> Credential failover enabled</label><div className="plan-list">{plans.map(x=><label key={x.id}><input type="checkbox" name="plan_ids" value={x.id}/> {x.name}</label>)}</div><button className="btn" disabled={busy==='new-provider'}>{busy==='new-provider'?'Creating…':'Create Provider'}</button></form>{message&&<div className="card">{message}</div>}<div className="provider-grid">{providers.map(p=><section className="card provider-card" key={p.id}><form className="stack" onSubmit={e=>updateProvider(e,p)}><div className="provider-card-head"><div><h2>{p.name}</h2><div className="muted">{p.slug} · {p.provider_type}</div></div><span className="pill">{p.enabled?'Enabled':'Disabled'}</span></div><div className="provider-form-grid"><input className="input" name="name" defaultValue={p.name}/><input className="input" name="slug" defaultValue={p.slug}/><input className="input" name="provider_type" defaultValue={p.provider_type}/><input className="input" name="base_url" defaultValue={p.base_url}/><input className="input" name="priority" type="number" defaultValue={p.priority}/><select className="input" name="credential_strategy" defaultValue={p.credential_strategy}><option value="priority">Priority / failover</option><option value="round_robin">Round robin</option></select></div><textarea className="input" name="description" defaultValue={p.description||''}/><div className="plan-list"><label><input type="checkbox" name="enabled" defaultChecked={p.enabled}/> Enabled</label><label><input type="checkbox" name="failover_enabled" defaultChecked={p.failover_enabled}/> Failover</label>{plans.map(x=><label key={x.id}><input type="checkbox" name="plan_ids" value={x.id} defaultChecked={p.provider_plan_access?.some(a=>a.plan_id===x.id&&a.enabled)}/> {x.name}</label>)}</div><div className="button-row"><button className="btn" disabled={busy===`provider-${p.id}`}>Save Provider</button><button className="btn danger" type="button" onClick={()=>{if(confirm(`Delete ${p.name}?`))run(`delete-provider-${p.id}`,()=>api('/api/v1/admin/providers','DELETE',{id:p.id}))}}>Delete Provider</button></div></form><hr/><h3>Credentials ({p.credentials.length})</h3><div className="credential-list">{p.credentials.map(c=><CredentialRow key={c.id} credential={c} run={run} setMessage={setMessage}/>)}</div><AddCredentialForm providerId={p.id} busy={busy} run={run}/></section>)}</div></div>}
